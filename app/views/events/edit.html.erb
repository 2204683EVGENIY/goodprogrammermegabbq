<h1><%= t("events.event.edit") %></h1>

<div class="row">
  <div class="col-md-6 buffer-top">
    <%= render "form" %>
  </div>
</div>

<%= button_to t("button.destroy_event"), event_path(@event), method: :delete, data: { confirm: t("events.event.destroy_confirm"), turbo_confirm: t("events.event.destroy_confirm") },  class: "btn btn-danger mt-3 lm-3" %>
