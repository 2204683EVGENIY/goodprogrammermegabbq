<h2><%= t("registrations.edit") %></h2>

<div class="col-lg-9">
  <%= form_for(resource, as: resource_name, url: registration_path(resource_name), data: { turbo: false }, html: { method: :put }) do |f| %>
    <%= render "devise/shared/error_messages", resource: resource %>

    <div class="form-group mt-3">
      <%= f.label :email %><br />
      <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
    </div>

    <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
      <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
    <% end %>

    <div class="form-group mt-3">
      <%= f.label :password %> <em class="form-text">(<%= t("devise.passwords.new_password") %>)</em><br />
      <%= f.password_field :password, autocomplete: "new-password" %>
    </div>

    <div class="form-group mt-3">
      <%= f.label :password_confirmation %> <em class="form-text">(<%= t("devise.passwords.new_password_confirmation") %>)</em><br />
      <%= f.password_field :password_confirmation, autocomplete: "new-password" %>
    </div>

    <div class="form-group mt-3">
      <%= f.label :current_password %> <em class="form-text">(<%= t("devise.passwords.live") %>)</em><br />
      <%= f.password_field :current_password, autocomplete: "current-password" %>
    </div>

    <div class="actions buffer-bottom mt-3">
      <%= f.submit t("devise.passwords.edit"), class: "btn btn-primary" %>
    </div>
  <% end %>

  <%= button_to t("button.delete_account"), registration_path(resource_name), method: :delete, data: { confirm: t("users.user.destroy_confirm"), turbo_confirm: t("users.user.destroy_confirm") },  class: "btn btn-danger mt-3 mb-3" %>

  <%= link_to t("link.back"), :back, class: "btn-link mt-3" %>
</div>
